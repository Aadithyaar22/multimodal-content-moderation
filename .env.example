# Copy to .env and fill in. Never commit .env.

# HuggingFace — needed for dataset/model downloads (a read token is enough)
HF_TOKEN=

# LLM explanation layer (Chapter 7) — pick one
GEMINI_API_KEY=
GROQ_API_KEY=

# Experiment tracking (Chapter 9)
WANDB_API_KEY=

# Moderation log store (Chapter 8)
MONGODB_URI=
